import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';
import './index.css';

var elem =ReactDOM.render(<App />, document.getElementById('root'));

ReactDOM.render(elem);
